import { StyleSheet, Text, View } from "react-native";
import React from "react";

import LoginScreen from "@/screens/UnAuthenticated/auth/login.screen";

export default function Login() {
  return <LoginScreen />;
}

const styles = StyleSheet.create({});
